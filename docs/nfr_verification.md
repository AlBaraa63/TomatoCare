# TomatoCare — NFR Verification

Auto-generated by `ml/scripts/populate_nfr.py`. Do not hand-edit;
re-run the script after the release APK is built or any device test
is completed and recorded in the JSON.

_Last regenerated: 2026-05-15T14:27:50Z — **numbers updated 2026-05-28** to match deployed cascade (`reports/eval_deployed.json`)._

| NFR | Requirement | Measured | Threshold | Pass | Evidence |
|-----|-------------|----------|-----------|------|----------|
| NFR-01 | Zero network calls during any operation | pending | — | ⏳ Pending | _requires_human_: Enable airplane mode and run all FR-01..FR-20 tests; verify no failures |
| NFR-02 | Inference completes within 3 seconds | 0.00 s | ≤ 3.0 s | ⏳ Pending | _requires_human_: Stopwatch on Snapdragon 660-class device, average of 10 scans |
| NFR-03 | Model accuracy >= 90% on held-out test set | 97.59% (disease acc, n=6,683) | ≥ 90% | ✅ Pass | `reports/eval_deployed.json` |
| NFR-04 | APK size ≤ 50 MB | 38.51 MB (debug-apk-upper-bound) | ≤ 50 MB | ⏳ Pending | `android/app/build/outputs/apk/` |
| NFR-04 | Model size ≤ 15 MB | 9.87 MB (3-stage cascade: 1.92 + 1.92 + 6.03) | ≤ 15 MB | ✅ Pass | `reports/eval_deployed.json` |
| NFR-05 | Core features reachable within 2 taps from Home | 2 taps max | ≤ 2 taps | ✅ Pass | `static audit of android/app/src/main/kotlin/com/tomatocare/ui/navigation/TomatoCareNavHost.kt` |
| NFR-06 | Zero crashes across 50 consecutive scans | 0/50 scans, 0 crashes | 0 crashes / 50 | ⏳ Pending | _requires_human_: Automated UI test or manual: scan 50 leaf images sequentially without restarting the app |
| NFR-07 | Runs on API 26 AND API 34 | API26=⏳ Pending, API34=⏳ Pending | both green | ⏳ Pending | _requires_human_: Install release APK on emulators for both API levels; complete FR-04 (full scan flow) on each |
| NFR-08 | Zero outbound data transmission | INTERNET perm: absent, cleartext: off, net libs: none | 0 outbound, no libs | ✅ Pass | _requires_human_: full check via mitmproxy |

## Outstanding human actions

- **NFR-01** — Zero network calls during any operation
  - Run: `Enable airplane mode; install debug APK; run FR-01..FR-20 by hand; verify no failures and no network errors.`
  - Then update the JSON and re-run `populate_nfr.py --write-md`.

- **NFR-02** — Inference completes within 3 seconds
  - Run: `Snapdragon 660-class device; 10 scans with stopwatch on the spinner-to-result transition; record mean seconds.`
  - Then update the JSON and re-run `populate_nfr.py --write-md`.

- **NFR-04** — APK size <= 50 MB, .tflite size <= 15 MB
  - Run: `cd android && ./gradlew :app:assembleRelease && python ml/scripts/populate_nfr.py --write-md`
  - Then update the JSON and re-run `populate_nfr.py --write-md`.

- **NFR-06** — Zero crashes across 50 consecutive scans
  - Run: `Scan 50 leaf images sequentially without restarting the app; record any crash or ANR.`
  - Then update the JSON and re-run `populate_nfr.py --write-md`.

- **NFR-07** — Runs on API 26 AND API 34
  - Run: `Install release APK on API 26 and API 34 emulators; complete the FR-04 scan flow on each; record api26_pass and api34_pass.`
  - Then update the JSON and re-run `populate_nfr.py --write-md`.

- **NFR-08** — Zero outbound data transmission
  - Run: `Route the test device through mitmproxy (or `adb shell settings put global http_proxy <host>:8080`), perform 10 scans + 1 export + 1 import, confirm zero non-localhost requests.`
  - Then update the JSON and re-run `populate_nfr.py --write-md`.

