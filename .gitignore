# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
venv/
env/
.venv/
build/
dist/
*.egg-info/
.env
.env.*

# Claude / Agent files
.claude/
.claude*

# ML artifacts and datasets
ml/dataset/
ml/models/checkpoints/*.keras
ml/models/checkpoints/*.h5
# ml/models/tflite/*.tflite
# Keep results/ JSON files (they're small and serve as a reference run record).

# Android
.gradle/
.idea/
build/
local.properties
captures/
*.iml
.cxx/
android/.gradle/
android/build/
android/app/build/
android/.idea/
android/local.properties
android/captures/
android/*.iml
android/app/*.iml
android/.cxx/

# Model weights copied into assets are not committed — too large (CLAUDE.md
# rule: no trained weights in git). Covers the 3-stage cascade + the old model.
# android/app/src/main/assets/*.tflite

# Document & Report Files (local/personal reference)
*.docx
report.txt

# OS
.DS_Store
Thumbs.db
desktop.ini

# Editors & IDEs
.vscode/
*.swp
*.swo
*~
